image: rocker/r-base:latest

stages:
  - check
  - test

variables:
  R_LIBS_USER: "$CI_PROJECT_DIR/ci/lib"

before_script:
  - apt-get update && apt-get install -y libcurl4-openssl-dev libssl-dev libxml2-dev
  - Rscript -e 'install.packages(c("devtools", "covr"), repos="https://cloud.r-project.org")'

cran-check:
  stage: check
  script:
    - Rscript -e 'devtools::check(document = FALSE, error_on = "warning")'
  artifacts:
    paths:
      - devtools-check.log
    expire_in: 1 week

test-coverage:
  stage: test
  script:
    - R -e 'cov <- covr::package_coverage(); covr::to_cobertura(cov)'
  artifacts:
    reports:
      coverage_report:
        coverage_format: cobertura
        path: cobertura.xml
  coverage: '/Coverage: \d+\.\d+/'
